{{ #if visible }}
<div id="header">
    <div class="wrapper">
        <div class="title">userde.sk/intermine</div>
        <div class="menu">
            <ul>
                {{ #each menu }}
                <li><a class="{{ #if active }}active{{ /if }}">{{ name }}</a></li>
                {{ /each }}
            </ul>
        </div>
    </div>
</div>
{{ /if }}