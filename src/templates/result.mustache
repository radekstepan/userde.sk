{{ #closed_at }}
<span class="tag closed">closed</span>
{{ /closed_at }}

<a target="issue_{{ number }}" href="{{ html_url }}" class="link">{{ title }}</a>

<span class="ago">{{ ago updated_at }}</span>